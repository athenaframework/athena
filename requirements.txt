# ATHENA - Core dependencies
# Install: pip install -r requirements.txt

# CLI & output
rich>=13.0.0

# Config
pyyaml>=6.0

# Data validation
pydantic>=2.0.0

# LLM providers
openai>=1.0.0          # OpenAI-compatible APIs (OpenAI, Groq, xAI, DeepSeek, Together, Fireworks)
ollama>=0.1.0          # Local Ollama client

# Dashboard API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Memory
chromadb>=0.5.0        # Optional: vector semantic search (falls back to SQLite-only if missing)

# Tools
gitpython>=3.1.0       # Git operations in ATHENATools
duckduckgo-search>=6.0 # Web search tool

# Bot interfaces
python-telegram-bot>=20.0
discord.py>=2.3.0
pywhatkit>=5.4         # WhatsApp (unofficial, browser-based – optional)

# Env vars
python-dotenv>=1.0.0

# MCP server
mcp>=1.0.0

# Testing (used by tester agent)
pytest>=8.0.0
