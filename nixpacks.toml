[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.build]
cmds = [
  "cd dashboard && npm install && npm run build && cd ..",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn backend.dashboard_api:app --host 0.0.0.0 --port $PORT"
